# Screenshots

Add pub.dev screenshots here before publishing and reference them in the
`screenshots:` block of `pubspec.yaml`.

Recommended captures (`.webp` preferred for size):

- `banner.webp` — an anchored/large anchored banner rendered in the example app.
- `native.webp` — the large native template rendered in the example app.

Keep images under ~200 KB each.
